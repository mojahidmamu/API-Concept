/**
 * 
 */
// templete string
const a = 56;
const numbers = [12, 20, 36];
const person = {
  name: 'Mojahid',
}
const message = `hello, ${person.name} has ${a} access to ${numbers[2]}`
console.log( message);

